#include "LiquidDensity.h"


//TODO: Implement both of these methods.  run_simulation() 
//should use allData to call computeDensity() on the pEOS 
//object and then update all data. output_results() should 
//print the results from allData in a nice way
int LiquidDensity::run_simulation(vector<double> &allData)
{
	/*int ntemp = GLOBAL::nTemp;
	int npres = GLOBAL::nPres;
	//for each temp and pres
	
	pEOS->computeDensity(feed, pComps, pres, temp, 0, den); */
	return 0; 

}

void LiquidDensity::output_results(vector<double> &allData){} 